import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  title: {
    fontSize: 12,
    color: '#4681A3',
    marginHorizontal: 8,
  },
  icon: {
    width: 40,
    height: 40,
  },
});

export default styles;
