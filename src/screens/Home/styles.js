import { StyleSheet } from "react-native"


const styles = StyleSheet.create({
    container: {
        margin: 32
    },
    subtitle: {
        color: '#000000',
        fontSize: 20,
        marginTop: 40,
        marginBottom: 18
    }
})

export default styles;