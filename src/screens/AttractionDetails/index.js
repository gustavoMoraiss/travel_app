import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const AttractionsDetails = () => {
  return (
    <SafeAreaView>
      <Text>Attraction Details Screen</Text>
    </SafeAreaView>
  );
};

export default React.memo(AttractionsDetails);
